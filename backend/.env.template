# =====================================================
# 🌸 Irisje.nl – Environment Template (voorbeeld)
# =====================================================
# Maak een kopie van dit bestand en noem het:  .env
# Vul alle waarden in voor jouw omgeving (Render, lokaal, etc.)
# =====================================================

# === 🌐 SERVER INSTELLINGEN ===
PORT=3000
NODE_ENV=production

# === 🧩 DATABASE ===
# Volledige MongoDB Atlas connectiestring:
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/irisje?retryWrites=true&w=majority

# === 🔐 AUTHENTICATIE ===
# Unieke geheime sleutel voor JWT-tokens
JWT_SECRET=vervang_dit_door_een_lang_random_geheim

# === ✉️ E-MAIL (SMTP via bijv. Sendinblue / Gmail / TransIP) ===
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=info@irisje.nl
SMTP_PASS=sterk_wachtwoord
SMTP_FROM="Irisje.nl <info@irisje.nl>"

# === 💳 BETAALSYSTEMEN (optioneel, toekomstig) ===
# Voor Stripe of Mollie integratie
STRIPE_SECRET_KEY=
MOLLIE_API_KEY=

# === ⚙️ OVERIGE OPTIES ===
# Render URL’s (gebruikt in CORS-instellingen)
FRONTEND_URL=https://irisje.nl
BACKEND_URL=https://irisje-backend.onrender.com

# === 📦 LOGGING & BEHEER ===
# true = extra logs tonen bij opstart (voor debuggen)
VERBOSE_LOGGING=false
